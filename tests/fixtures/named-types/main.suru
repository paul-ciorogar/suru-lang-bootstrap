type Point: { x Int64, y Int64 }

type Person: {
    name String
    age Int64
}

fn main(args Array<String>) {
    let p Point: { x: 2283, y: 2281 }
    printLn(p.x)
    printLn(p.y)

    let person Person: { name: "Suru", age: 1 }
    printLn(person.name)
    printLn(person.age)
}
